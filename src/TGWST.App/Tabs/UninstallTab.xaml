<UserControl x:Class="TGWST.App.Tabs.UninstallTab" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
<Grid Margin="8">
<Grid.RowDefinitions>
<RowDefinition Height="Auto"/>
<RowDefinition Height="2*"/>
<RowDefinition Height="*"/>
<RowDefinition Height="Auto"/>
</Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
        <Button Content="Refresh" Click="Refresh_Click" Margin="0,0,8,0"/>
        <Button Content="Uninstall Selected" Click="Uninstall_Click" Margin="0,0,8,0"/>
        <Button Content="Remove Leftovers" Click="RemoveLeftovers_Click" Margin="0,0,8,0"/>
        <CheckBox x:Name="DryRunCheck" Content="Dry run (no delete)" VerticalAlignment="Center"/>
    </StackPanel>

    <DataGrid x:Name="AppsGrid"
              Grid.Row="1"
              ItemsSource="{Binding Apps}"
              AutoGenerateColumns="False"
              IsReadOnly="True">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="*"/>
            <DataGridTextColumn Header="Publisher" Binding="{Binding Publisher}" Width="2*"/>
        </DataGrid.Columns>
    </DataGrid>

    <DataGrid x:Name="LeftoversGrid"
              Grid.Row="2"
              ItemsSource="{Binding Leftovers}"
              AutoGenerateColumns="False"
              IsReadOnly="False">
        <DataGrid.Columns>
            <DataGridCheckBoxColumn Header="Delete?" Binding="{Binding Selected, Mode=TwoWay}" Width="Auto"/>
            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="Auto"/>
            <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*"/>
            <DataGridTextColumn Header="Size" Binding="{Binding SizeDisplay}" Width="Auto"/>
            <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="2*"/>
        </DataGrid.Columns>
    </DataGrid>

    <TextBlock Grid.Row="3"
               Text="{Binding Status}"
               Margin="0,8,0,0"/>
</Grid>
</UserControl>
