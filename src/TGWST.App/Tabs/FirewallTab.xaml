<UserControl x:Class="TGWST.App.Tabs.FirewallTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock x:Name="SummaryText" VerticalAlignment="Center" FontWeight="Bold"/>
            <Button Content="Refresh" Margin="8,0,0,0" Click="Refresh_Click"/>
            <Button Content="Open Connections Monitor" Margin="8,0,0,0" Click="OpenConnections_Click" />
        </StackPanel>

        <DataGrid x:Name="ProfilesGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Profile" Binding="{Binding Profile}" Width="*"/>
                <DataGridTextColumn Header="State" Binding="{Binding State}" Width="Auto"/>
                <DataGridTextColumn Header="Policy" Binding="{Binding Policy}" Width="2*"/>
                <DataGridCheckBoxColumn Header="Vulnerable" Binding="{Binding IsVulnerable}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="2" Header="Region Lock (Inbound Geo-Block)" Margin="0,8,0,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock VerticalAlignment="Center" Text="Checked = blocked. Requires Administrator."/>
                    <Button x:Name="RegionApplyButton" Content="Apply" Margin="8,0,0,0" Click="RegionApply_Click"/>
                    <TextBlock x:Name="RegionStatusText" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="Gray"/>
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="190">
                    <ItemsControl x:Name="RegionCountriesList">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Label}"
                                          IsChecked="{Binding IsBlocked, Mode=TwoWay}"
                                          Margin="0,0,14,6"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </GroupBox>

        <TextBlock x:Name="ErrorText"
                   Grid.Row="3"
                   Margin="0,8,0,0"
                   Foreground="Red"/>
    </Grid>
</UserControl>
