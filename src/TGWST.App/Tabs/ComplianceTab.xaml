<UserControl x:Class="TGWST.App.Tabs.ComplianceTab" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox x:Name="BaselinePathBox" Width="400" Margin="0,0,8,0" />
            <Button Content="Evaluate" Click="Evaluate_Click"/>
        </StackPanel>

        <DataGrid x:Name="ResultsGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Hive" Binding="{Binding Item.Hive}" Width="Auto"/>
                <DataGridTextColumn Header="Path" Binding="{Binding Item.Path}" Width="*"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Item.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Expected" Binding="{Binding Item.Value}" Width="Auto"/>
                <DataGridTextColumn Header="Current" Binding="{Binding CurrentValue}" Width="Auto"/>
                <DataGridCheckBoxColumn Header="Compliant" Binding="{Binding Compliant}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="StatusText" Grid.Row="2" Margin="0,8,0,0" Text="Ready"/>
    </Grid>
</UserControl>
