<Window x:Class="TGWST.App.Windows.BootstrapWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TGWST - First Run Provisioning"
        Width="980"
        Height="760"
        MinWidth="900"
        MinHeight="680"
        Background="#1A1F2B"
        FontFamily="Cascadia Mono, Consolas"
        WindowStartupLocation="CenterScreen"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="Aliased"
        TextOptions.TextHintingMode="Fixed">
    <Window.Resources>
        <SolidColorBrush x:Key="Panel" Color="#21293A" />
        <SolidColorBrush x:Key="Panel2" Color="#1D2535" />
        <SolidColorBrush x:Key="Line" Color="#3C4C68" />
        <SolidColorBrush x:Key="TextPrimary" Color="#DEE7F6" />
        <SolidColorBrush x:Key="TextMuted" Color="#94A4C1" />
        <SolidColorBrush x:Key="Green" Color="#8ED26D" />
        <SolidColorBrush x:Key="Amber" Color="#D9BA70" />
        <SolidColorBrush x:Key="Red" Color="#E16E74" />
        <SolidColorBrush x:Key="Blue" Color="#64A7E2" />
        <SolidColorBrush x:Key="Cyan" Color="#7EC7FF" />
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="8,6" Background="{StaticResource Panel}" BorderBrush="{StaticResource Line}" BorderThickness="1">
            <StackPanel>
                <TextBlock FontSize="10" Foreground="{StaticResource Cyan}" Text="[TGWST FIRST-RUN PROVISIONING WIZARD]" />
                <TextBlock Margin="0,2,0,0" FontSize="10" Foreground="{StaticResource TextPrimary}" Text="Provision WSL2 + Ubuntu + Docker + Pi-hole + sudo credentials for this machine." />
                <TextBlock Margin="0,1,0,0" FontSize="9" Foreground="{StaticResource TextMuted}" Text="Run this once after MSI install on new Windows 11 systems." />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Margin="0,6,0,0" Padding="8,6" Background="{StaticResource Panel2}" BorderBrush="{StaticResource Line}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" FontSize="9" Foreground="{StaticResource Blue}" Text="WSL2 / Ubuntu" />
                <TextBlock x:Name="WslStatusText" Grid.Row="0" Grid.Column="1" FontSize="9" Foreground="{StaticResource TextPrimary}" Text="-" />

                <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,2,0,0" FontSize="9" Foreground="{StaticResource Blue}" Text="Docker" />
                <TextBlock x:Name="DockerStatusText" Grid.Row="1" Grid.Column="1" Margin="0,2,0,0" FontSize="9" Foreground="{StaticResource TextPrimary}" Text="-" />

                <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,2,0,0" FontSize="9" Foreground="{StaticResource Blue}" Text="Pi-hole Appliance" />
                <TextBlock x:Name="PiHoleStatusText" Grid.Row="2" Grid.Column="1" Margin="0,2,0,0" FontSize="9" Foreground="{StaticResource TextPrimary}" Text="-" />

                <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,2,0,0" FontSize="9" Foreground="{StaticResource Blue}" Text="FTL Telemetry (4711)" />
                <TextBlock x:Name="FtlStatusText" Grid.Row="3" Grid.Column="1" Margin="0,2,0,0" FontSize="9" Foreground="{StaticResource TextPrimary}" Text="-" />

                <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,2,0,0" FontSize="9" Foreground="{StaticResource Blue}" Text="DNS Target" />
                <TextBlock x:Name="DnsTargetText" Grid.Row="4" Grid.Column="1" Margin="0,2,0,0" FontSize="9" Foreground="{StaticResource TextPrimary}" Text="-" />

                <TextBlock Grid.Row="5" Grid.Column="0" Margin="0,2,0,0" FontSize="9" Foreground="{StaticResource Blue}" Text="Sudo Credentials" />
                <TextBlock x:Name="CredsStatusText" Grid.Row="5" Grid.Column="1" Margin="0,2,0,0" FontSize="9" Foreground="{StaticResource TextPrimary}" Text="-" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Margin="0,6,0,0" Padding="8,6" Background="{StaticResource Panel}" BorderBrush="{StaticResource Line}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock FontSize="9" Foreground="{StaticResource Cyan}" Text="[RUN PLAN]" />
                    <CheckBox x:Name="EnsureWslCheck" Margin="0,4,0,0" Foreground="{StaticResource Green}" IsChecked="True" Content="[✓] Ensure WSL2 + Ubuntu-22.04" />
                    <CheckBox x:Name="InstallDockerCheck" Margin="0,2,0,0" Foreground="{StaticResource Green}" IsChecked="True" Content="[✓] Install Docker engine in Ubuntu" />
                    <CheckBox x:Name="InstallPiHoleCheck" Margin="0,2,0,0" Foreground="{StaticResource Green}" IsChecked="True" Content="[✓] Install Pi-hole Docker appliance + service" />
                    <CheckBox x:Name="SyncDnsCheck" Margin="0,2,0,0" Foreground="{StaticResource Green}" IsChecked="True" Content="[✓] Sync Windows DNS to current WSL IP" />
                    <Grid Margin="0,2,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock VerticalAlignment="Center" FontSize="9" Foreground="{StaticResource TextMuted}" Text="Interface alias:" />
                        <TextBox x:Name="InterfaceAliasBox" Grid.Column="1" Height="24" Padding="4,1" Text="Ethernet" />
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock FontSize="9" Foreground="{StaticResource Cyan}" Text="[SUDO CREDENTIAL CACHE]" />
                    <CheckBox x:Name="SaveCredsCheck" Margin="0,4,0,0" Foreground="{StaticResource Green}" IsChecked="True" Content="[✓] Save encrypted sudo credentials (DPAPI)" />
                    <Grid Margin="0,4,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock VerticalAlignment="Center" FontSize="9" Foreground="{StaticResource TextMuted}" Text="Linux username:" />
                        <TextBox x:Name="UsernameBox" Grid.Column="1" Height="24" Padding="4,1" />
                    </Grid>
                    <Grid Margin="0,2,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock VerticalAlignment="Center" FontSize="9" Foreground="{StaticResource TextMuted}" Text="Sudo password:" />
                        <PasswordBox x:Name="PasswordBox" Grid.Column="1" Height="24" Padding="4,1" />
                    </Grid>
                    <TextBlock Margin="0,4,0,0" FontSize="9" Foreground="{StaticResource TextMuted}" Text="Leave password blank to keep existing stored password." />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="3" Margin="0,6,0,0" Padding="8,6" Background="#141A27" BorderBrush="{StaticResource Line}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" FontSize="9" Foreground="{StaticResource Cyan}" Text="[PROVISION LOG]" />
                <TextBox x:Name="LogBox"
                         Grid.Row="1"
                         Margin="0,4,0,0"
                         FontSize="10"
                         Foreground="{StaticResource TextPrimary}"
                         Background="#0F1523"
                         BorderBrush="#2E3D5A"
                         BorderThickness="1"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         AcceptsReturn="True" />
            </Grid>
        </Border>

        <Border Grid.Row="4" Margin="0,6,0,0" Padding="8,4" Background="{StaticResource Panel}" BorderBrush="{StaticResource Line}" BorderThickness="1">
            <DockPanel>
                <TextBlock x:Name="FooterStatusText" FontSize="9" Foreground="{StaticResource TextMuted}" Text="Ready." />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button x:Name="RefreshButton" Width="92" Margin="0,0,6,0" Content="Refresh" Click="RefreshButton_Click" />
                    <Button x:Name="RunButton" Width="142" Margin="0,0,6,0" Content="Run Selected Setup" Click="RunButton_Click" />
                    <Button Width="92" Content="Continue" Click="ContinueButton_Click" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
